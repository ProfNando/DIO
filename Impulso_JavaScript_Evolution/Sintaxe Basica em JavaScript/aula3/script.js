//O que são vetores e Arrays

let array = ['String', 1, false]
console.log(array);

let array1 = ['Ola', 343.4, false, [array], ['tudo bem ', 43,true]] 
console.log(array1);
//Manipulando Arrays

/**
 * forEach() - > itera um array
 * push() - > add item no final do array
 * pop() - > remove item no final do array
 * shift() - > remove item no inicio do array
 * unshift - > add item no inicio do array
 * indexOf() - > retorna o indice de um valor
 * splice() - > remove ou substitui um iten pelo indice
 * slice -> retorna uma parte de um array existente
 */
/**
 * Objetos - > Possuem propriedades 
 * e valores que definem suas 
 * características. 
 * Devem ser declarado entre chaves'{}'
 * 
 */

4!=6;